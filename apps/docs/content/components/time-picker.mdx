---
title: TimePicker
description: Selects a time of day with hour, minute, and AM/PM selects.
---

<!-- This file is generated by scripts/generate-docs.ts. Do not edit manually. -->

# TimePicker

Selects a time of day with hour, minute, and AM/PM selects.

## Install

```bash
pnpm dlx @stalk-ui/cli add @stalk-ui/time-picker
```

## Examples

<ComponentPreview
  slug="time-picker"
  example={0}
  code={`<TimePicker hourCycle="12" minuteStep={5} />`}
/>

```tsx
<TimePicker hourCycle="12" minuteStep={5} />
```

<ComponentPreview
  slug="time-picker"
  example={1}
  code={`<TimePicker hourCycle="24" minuteStep={15} />`}
/>

```tsx
<TimePicker hourCycle="24" minuteStep={15} />
```

## Props

| Prop          | Type                      | Required | Default | Description                                                                     |
| ------------- | ------------------------- | -------- | ------- | ------------------------------------------------------------------------------- |
| `value`       | `string`                  | No       | -       | Time as a 24-hour \`HH:mm\` string (controlled).                                |
| `onChange`    | `(value: string) => void` | No       | -       | Called with the updated 24-hour \`HH:mm\` string.                               |
| `hourCycle`   | `'12' \| '24'`            | No       | -       | Display the hours as a 12-hour clock with an AM/PM selector (default \`'12'\`). |
| `minuteStep`  | `number`                  | No       | -       | Granularity of the minutes options in minutes (default \`1\`).                  |
| `size`        | `SelectSize`              | No       | -       | -                                                                               |
| `disabled`    | `boolean`                 | No       | -       | -                                                                               |
| `className`   | `string`                  | No       | -       | -                                                                               |
| `hourLabel`   | `string`                  | No       | -       | -                                                                               |
| `minuteLabel` | `string`                  | No       | -       | -                                                                               |
| `periodLabel` | `string`                  | No       | -       | -                                                                               |

## Variants

No recipe variants are documented for this component.

## Registry

- Manifest: `/r/time-picker.json`
- Recipe: `timePicker`
- Files: `src/components/ui/time-picker.tsx`
