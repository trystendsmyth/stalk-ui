---
title: Badge
description: Displays compact status or metadata.
---

<!-- This file is generated by scripts/generate-docs.ts. Do not edit manually. -->

# Badge

Displays compact status or metadata.

## Install

```bash
pnpm dlx @stalk-ui/cli add @stalk-ui/badge
```

## Examples

<ComponentPreview slug="badge" example={0} code={`<Badge>Published</Badge>`} />

```tsx
<Badge>Published</Badge>
```

<ComponentPreview
  slug="badge"
  example={1}
  code={`<Badge variant="solid" tone="success">Live</Badge>`}
/>

```tsx
<Badge variant="solid" tone="success">
  Live
</Badge>
```

<ComponentPreview
  slug="badge"
  example={2}
  code={`<Badge variant="outline" tone="danger" size="sm" radius="sm">Error</Badge>`}
/>

```tsx
<Badge variant="outline" tone="danger" size="sm" radius="sm">
  Error
</Badge>
```

## Props

| Prop      | Type           | Required | Default | Description                                                                   |
| --------- | -------------- | -------- | ------- | ----------------------------------------------------------------------------- |
| `radius`  | `BadgeRadius`  | No       | -       | Border radius. Defaults to \`full\` (pill).                                   |
| `size`    | `BadgeSize`    | No       | -       | -                                                                             |
| `tone`    | `BadgeTone`    | No       | -       | Selects the semantic color palette used by the badge. Defaults to \`accent\`. |
| `variant` | `BadgeVariant` | No       | -       | -                                                                             |

## Variants

| Variant   | Values                           |
| --------- | -------------------------------- |
| `variant` | `solid`, `subtle`, `outline`     |
| `size`    | `sm`, `md`                       |
| `radius`  | `none`, `sm`, `md`, `lg`, `full` |

## Registry

- Manifest: `/r/badge.json`
- Recipe: `badge`
- Files: `src/components/ui/badge.tsx`
