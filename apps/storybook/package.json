{
  "name": "@stalk-ui/storybook",
  "version": "0.0.0",
  "private": true,
  "description": "Storybook workspace for Stalk UI.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --filter @stalk-ui/i18n build && pnpm --filter @stalk-ui/preset build && panda codegen && panda cssgen",
    "build": "storybook build --output-dir storybook-static",
    "clean": "rimraf storybook-static styled-system .lost-pixel/current .lost-pixel/difference",
    "predev": "pnpm --filter @stalk-ui/i18n build && pnpm --filter @stalk-ui/preset build && panda codegen && panda cssgen",
    "dev": "storybook dev --port 6006",
    "lint": "eslint .",
    "test": "pnpm --workspace-root check-storybook-a11y",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stalk-ui/i18n": "workspace:^",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@pandacss/dev": "catalog:",
    "@stalk-ui/preset": "workspace:*",
    "@storybook/addon-a11y": "^10.3.5",
    "@storybook/addon-themes": "^10.3.5",
    "@storybook/addon-vitest": "^10.3.5",
    "@storybook/react-vite": "^10.3.0",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "^6.0.1",
    "storybook": "catalog:",
    "vite": "^8.0.10"
  }
}
