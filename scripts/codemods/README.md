# Stalk UI Codemods

Internal codemods for registry and component migrations live here. Each migration should be
versioned by the schema or package version it upgrades from and must include fixture coverage before
it is referenced by the CLI.
