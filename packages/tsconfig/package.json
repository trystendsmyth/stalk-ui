{
  "name": "@stalk-ui/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configurations for Stalk UI.",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./base": "./base.json",
    "./react": "./react.json",
    "./react-library": "./react-library.json"
  },
  "scripts": {
    "build": "pnpm typecheck",
    "lint": "echo \"No tsconfig lint configured yet\"",
    "test": "echo \"No tests for @stalk-ui/tsconfig\"",
    "typecheck": "pnpm --workspace-root exec tsc --noEmit --project tsconfig.json"
  }
}
